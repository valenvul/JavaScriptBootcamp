// cuando declaras una variable sin definirla se le aplica el valor undefined

// si no se define un return en una funcion se devuelve undefined. Se pueden definir funciones sin return, no se les asigna tipo.

